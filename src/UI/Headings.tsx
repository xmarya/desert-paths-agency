import styled from "styled-components";

export const HeroHeading = styled.h1`
  font-size: var(--hero-heading);
  color: var(--colour-primary-dark-brown);
  font-weight: 700;
  line-height: 1.1;
  margin-block: 0 1.6rem;
`;

export const HeroSubHeading = styled.h3`
  font-size: var(--hero-sub-heading);
  color: var(--colour-secondary-text);
  margin-block: 0 4rem;
`;

export const SectionHeading = styled.h2`
color: var(--colour-primary-dark-brown);
  font-size: var(--section-heading);
`;

export const SectionSubHeading = styled.h4`
  font-size: var(--section-sub-heading);
  color: var(--colour-secondary-text);
  margin-top: var(--section-subheadings-mt);
`;